{
  "buildCommand": "npm run build && cp -r data dist/data",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/data/(.*)", "destination": "/data/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
